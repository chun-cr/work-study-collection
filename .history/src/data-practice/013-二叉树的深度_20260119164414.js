class treeNode{
    constructor(val, left = null, right = null){
        this.val = val
        this.left = left
        this.right = right
    }
}
function maxDepth(root){
    if (root === null) return
}